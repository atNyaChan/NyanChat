# Nyanw 版本改动记录

本文记录基于上游 RikkaHub 所做的个人定制，覆盖以下 WIP 提交：

- `ec12aa6` — `WIP: improve chat, backup, stats, and web server UX`
- `a7689cd` — `WIP: refine message stats and settings defaults`
- `efb0d01` — `WIP: refine backups, pricing, and message stats`

## Codex 协作约定

- 仓库级 `AGENTS.md` 新增 Codex 收尾流程：每次代码、资源或配置修改后同步维护本文件。
- 修改完成后要静态检查本次改动涉及的未引用组件及 locale/string 资源并清理。
- 禁止 Codex 自行执行编译、构建、测试或 Lint；相关验证由维护者手动完成。

## 聊天与消息

### 助手切换

- 修复切换助手后，在空白聊天页发送的第一条消息偶尔被写入上一个助手的问题。
- 修复方式为在创建新会话时明确使用当前助手，避免异步状态更新期间读取到旧助手。
- 该问题触发概率较低，仍需在日常使用中继续观察。

### 词数及生成统计

- 用户消息的操作按钮行左侧显示词数；助手消息词数包含正文和思考过程。
- 相邻 ASCII 英文字母合计为一个词；其他 ASCII 符号、空格、CJK 字符和 emoji 等每个 Unicode 码点各计一个词。
- 流式回复期间每 100ms 更新词数、已用时间、词/秒及生成中的思考词数。
- 生成完成后助手统计变为两行：第一行显示词数、时间、词/秒，第二行显示输入（缓存 token）、输出、token/秒及可能的费用。
- 点击统计只切换精确数字和 `12.3K` 一类的缩写；仅数量超过 `10000` 时才采用缩写，并且无论数量多大都只使用 `K`。
- 用户消息和思考块的词数统计使用同样的浅灰色样式、`word` 单位及缩写规则。
- 手动编辑且没有生成用量的助手消息只显示词数。
- 已结束且没有生成用量的助手消息按手动编辑消息处理；其中的零耗时思考块只显示“思考”，自然生成且带有用量信息的零耗时思考仍显示“思考了 0.0 秒”。
- 消息用时不足 150ms 时不显示 `word/s` 和 `tok/s`，避免展示失真的瞬时速度。

### 编辑助手消息中的思考

- 编辑助手消息时，将 `Reasoning` 内容重新插入输入框并表示为 `<think>...</think>`。
- 保存编辑后的助手消息时，将 `<think>` 标签重新解析为独立思考块。
- 支持一条编辑文本中出现多个 `<think>` 块。
- 思考块与正文之间不再自动插入新的换行，避免反复编辑后继续累积。

### 思考块

- 增加“复制思考过程”按钮，复制内容为 Markdown 原文。
- 思考完成并展开思考块后，顶部显示思考词数和复制按钮。

### 空白消息

- 允许发送仅包含空格等空白字符的消息。
- 同步调整了输入校验、消息 UI 判断及发送给模型前的消息过滤。
- 真正的空字符串仍不能作为单独消息发送。
- 纯空白用户消息使用不可见占位符维持正常气泡行高；该占位符仅用于显示，不改变保存或发送的原始内容。

## 模型价格

- 每个提供商的每个模型均可选中“设置价格”。
- 支持填写以下价格，单位为美元/百万 token：
  - 输入
  - 输出
  - 缓存读取
  - 缓存写入
- 价格设置默认关闭，旧模型数据因字段具有默认值而保持兼容。
- 未出现缓存读取时，费用计算为：

  ```text
  输入价格 × 输入 token + 输出价格 × 输出 token
  ```

- 出现缓存读取时，不使用普通输入价格，费用计算为：

  ```text
  缓存写入价格 ×（输入 token - 缓存读取 token）
  + 缓存读取价格 × 缓存读取 token
  + 输出价格 × 输出 token
  ```

- 最终结果除以 `1,000,000`，并显示在助手消息统计末尾。

## 默认提供商

- 从新安装的内置聊天提供商列表中移除：
  - RikkaHub
  - 小马算力
  - 阶跃星辰
  - 腾讯混元
  - 随想 AI 网关
  - AckAI
- 从推荐提供商中移除随想 AI 网关。
- 只调整新安装时的默认数据，不迁移或删除已有安装中用户保存的提供商设置。
- 阶跃星辰 TTS 属于独立的语音提供商配置，本次没有删除。

## 备份与恢复

- 数据设置中的入口名称改为“备份与恢复”。
- 描述改为“备份或覆盖还原整个RikkaHub”。
- “聊天记录存储”改为“聊天文件存储”。
- 本地、WebDAV、S3 的覆盖还原操作均增加确认弹窗。用户必须在提示现有配置和数据会被覆盖后点击“好”，才会进入文件选择或开始还原。
- ChatBox、Cherry Studio 等合并式导入不属于覆盖还原，因此不显示覆盖警告。
- 本地完整备份和还原固定包含数据库与文件，不再受 WebDAV 备份项目勾选状态影响。
- 导入完整备份后扫描消息数据库，将图片记录中的旧绝对 `file://` 路径按文件名改写为当前安装 `upload` 目录的路径。
- 新导出格式改为 `.tar`：
  - 导出前对 SQLite WAL 执行 checkpoint，归并到主数据库。
  - 数据库只导出一个 `rikka_hub.db.zst`，不再携带 `-wal` 和 `-shm`。
  - 数据库使用 zstd 等级 9 压缩，再与 `settings.json`、`upload`、skills、fonts 等附件一起打入 tar。
  - 本地、WebDAV 和 S3 使用相同的新格式。
- 导入根据文件后缀识别新 `.tar` 与旧 `.zip`，旧 ZIP 备份仍可还原。
- 本地导出额外提供旧 ZIP 兼容格式；S3 与 WebDAV 仍只导出新的 TAR 格式。
- Android 端 zstd 使用官方 `zstd-jni` AAR（包含各 Android ABI 原生库），避免普通 JVM JAR 缺少 `.so` 导致导出失败。
- Release 混淆配置保留 zstd-jni 的 JNI 字段与方法名，避免导出时因字段被重命名而报 `dstPos` 缺失。
- 本地导入与导出的失败提示分开显示为“导入失败”和“导出失败”。
- 完整恢复前关闭 Room 数据库并清理目标数据库的旧 WAL/SHM，避免旧连接覆盖刚恢复的对话数据。
- 备份页签顺序调整为本地、S3、WebDAV、提醒。
- 设置页的备份说明注明“不包含工作区环境”。
- 导出本地、WebDAV 或 S3 备份前先写入最新备份时间，使归档内的设置也包含该时间；导出或上传失败时恢复导出前的时间。
- 备份进行期间会锁定备份页返回操作。
- 本地及第三方导入时显示不可通过返回键关闭的“正在导入”对话框，框内使用横向直线进度条，完成后同一对话框切换为重启提示；备份页导入项目不再显示进度动画，页面也不额外锁定返回键。

## 更新、提醒与请求日志

- 永久移除应用内自动更新检查、更新下载卡片及官网更新接口请求。
- 完整删除捐赠功能，包括页面与路由、Kofi/爱发电入口和图片、赞助名单 API/模型、DataStore 遗留字段、调试项及相关字符串资源，以及启动次数达到阈值后自动弹出的捐赠提醒。
- 请求日志永久开启并移除开关；日志仍仅保存在内存中，重启应用后清空。
- 请求和对应响应合并为同一个条目，记录双方 headers、正文、HTTP 状态和耗时。
- 请求失败只记录单行错误摘要，不在请求日志页显示 Java 日志或异常堆栈。
- 无请求记录时在页面中央显示灰色空状态提示。
- 请求/响应 headers 与正文均提供一键复制；JSON 和 SSE 中的 JSON 数据支持树形高亮与折叠，且默认从根节点折叠，点击根节点后再展开完整内容。
- 仅对识别出的 LLM 响应显示 Content，并按流事件顺序将正文、思考和工具原文拼成一段可复制的纯文本，使用 `[思考]`、`[正文]`、`[工具]` 标注并以空行分隔。
- 日志详情、请求/响应 headers 和 body 默认折叠，仅完整响应 Content 默认展开，字号统一为 12sp（行高 16sp）。
- 日志只展示文本响应；gzip、deflate、Brotli 和 zstd 编码的文本先解压。
- 流式响应在结束标记或 EOF 前被手动中断时，保留 HTTP 状态、响应头和已接收正文，并额外记录单行错误“流式响应已中断”；正常结束后的内部取消不报错。

## 聊天交互

- 侧栏移除“聊天历史”入口及其独立页面、ViewModel 和导航代码，同时移除底部重复的助手入口。
- 侧栏原“生成”菜单拆为独立的“翻译”和“图像生成”入口。
- 搜索聊天页增加“仅匹配标题”、“精准匹配”、“模糊搜索”三种模式；默认使用精准匹配并记住用户选择。精准匹配使用区分大小写的连续字符串包含判断，模糊搜索保留原 FTS/Jieba 实现。
- 助手消息的翻译入口移入三点操作面板，位于“编辑”下方、“分享”上方。
- 删除消息操作中的“使用网页视图渲染”及其消息级预览代码。
- 删除单条消息、单个聊天和批量聊天均增加确认框。
- 默认用户与助手头像使用应用兔子图标，移除原程序化渐变头像。
- 聊天列表支持多选，并可批量移动到文件夹、移动到助手或删除。
- 聊天列表的多选入口使用 `CheckList` 图标。
- 聊天列表长按菜单的标题操作改为“编辑标题”；编辑对话框按钮按取消/自动生成/确认分别左、中、右排列，自动生成后可由用户决定是否保存。
- 点击对话页顶部的标题时使用同样的编辑框。
- 分享面板第一行并排选择 Markdown 或图片，第二行统一设置是否保留思考，底部使用一个导出按钮。
- 点击回复下方的重试后立即创建并展示新的空助手分支，计时从点击时开始；连接失败时删除该分支并切回原回复。
- 默认兔子头像固定缩放为头像区域的 90%。
- 多选状态使用叉号退出并显示“已选x项”。
- 单个和批量聊天统一通过“移动到...”树，选择助手下的“未归类”虚拟文件夹或实际文件夹，并可从助手标题右侧现场新建文件夹；执行移动前会再次确认。
- 长按聊天菜单中的“移动到...”紧跟在“多选”之后。

## 统计页

- 聊天热力图从实际首次使用日期附近开始绘制，不再保留大量无数据区域。
- 最左侧保留一个星期的空白，便于观察起始边界。

## 翻译与图像生成

- 翻译页和图像生成页改用与收藏、统计页一致的大标题栏、页面背景和卡片层级。
- 翻译输入与结果分别置于卡片中；图像生成的结果区、输入区和图库卡片统一圆角与配色，并增加空结果提示。
- 翻译结果使用较输入标题更小的字号。

## Web 服务器与 mDNS

- 地址区域只保留一个“网址”项目。
- 展示手机当前可用的全部局域网 IPv4 地址，每行一个。
- 不显示 `localhost`、`127.0.0.1` 或 IPv6 地址。
- 复制网址功能会复制当前展示的有效地址。
- Web 服务器启动后注册 mDNS，可通过对应的 `.local` 地址访问。
- 端口和密码输入框字号适当缩小，并使用正常字重，避免视觉上过大或过粗。

## 设置页与默认值

- 请求日志默认开启，并移动到“模型与服务”分组末尾。
- “关于”分组只保留“关于”入口。
- 删除设置页中的赞助、分享、使用文档入口。
- 删除“关于”入口右侧的 QQ 和 Discord 链接。
- 常规偏好默认值调整：
  - 消息导航按钮默认关闭。
  - 自动滚动默认关闭。
  - 用户头像默认不显示。
  - 模型名称默认不显示。
- 删除“使用 App 风格指示器”开关，并永久启用该样式。
- 新建助手的内置工具默认全部关闭。
- 新安装默认关闭动态取色。
- DeepSeek 余额读取 `balance_infos` 中的 CNY 与 USD 总余额，并按 `x.xCNY+x.xUSD` 显示。
- 提供商“模型”页底部操作改为两行：第一行“自定义模型”，第二行高亮的“选择模型”；两个按钮按照较宽者统一宽度。
- 提供商页完整移除“推荐提供商”入口、推荐列表及其预置数据。
- 应用自身的显示名称统一改为 `NyanChat`，包括 Android 应用名、Web 客户端、Web 服务、导出图片水印及对外客户端标识；“备份与恢复”页的格式兼容说明，以及独立的 `RikkaHub` 搜索服务品牌保留原名。
- 新建应用时预置的两个助手均使用空 system prompt。
- 增加应用语言下拉框，默认跟随系统，并支持英语、简体中文、繁体中文、日语、韩语和俄语。
- 关于页的 Fork GitHub 与项目官网统一指向 `https://github.com/atNyaChan/NyanChat`；页面最底部单独保留 `https://rikka-ai.com`，标注为“原项目官网”。
- 关于页的许可证链接改为本 Fork 仓库中的 `LICENSE` 文件。
- 关于页底部增加英文修改来源、修改者与 AGPL-3.0 许可声明。
- `locale-tui-localization` skill 改为手工维护全部 `values*` 字符串资源，不再依赖 Python、uv 或自动翻译服务。
- 删除未引用的字符串，并使得各翻译资源与英文源文件同步。
- 助手的“上下文消息数量”由滑条改为正整数输入框，留空表示不限制发送给模型的历史消息数量，并删除限制上下文后的红色警告。
- 助手“最大 Token 数”输入框不再重复显示灰色限制提示。
- 助手背景不透明度滑条调整为 10%–100%，每 10% 一档。
- “偏好设置”的入口描述改为“界面、主题和常规设置”；“动态颜色”从“更多设置”移至“主题”页首项，启用时提示关闭动态颜色后才能修改主题。
- 自定义主题颜色不再使用三个 HSL 滑条和一个合并文本框，改为独立的 H、S、L 数字输入框；H 范围为 0–360，S/L 范围为 0–100。
- 消息导航按钮、生成通知、音量键翻页和聊天字体下拉框按最长选项自适应宽度；聊天字体下拉框移至项目右侧，并使用当前选中的字体渲染 `Aa` 预览，自定义字体未加载时回退系统默认字体。
- 助手的“上下文消息数量”、“最大 Token 数”和“粘贴长文本为文件”统一使用独占一行的数字输入框，前两者留空表示无限制，后者留空表示关闭

### “偏好设置”完整项目顺序

1. 显示用户头像（默认：关闭）
2. 显示模型名称（默认：关闭）
3. 显示消息日期时间（默认：关闭）
4. 显示 Token 和上下文统计（默认：开启）
5. 消息导航按钮（默认：关闭；可选：关闭、右、左）
6. 主题（默认：樱花粉；动态颜色默认：关闭）
7. 更多设置（默认值见下方完整列表）

### “更多设置”完整项目顺序

页面按以下顺序显示，每一项均标注新安装时的默认值：

1. 无标题常规分组
   1. 语言（默认：跟随系统）
   2. 启动时新建对话（默认：开启）
   3. 按 Enter 发送（默认：关闭）
   4. 自动滚动（默认：关闭）
   5. 启用模糊效果（默认：关闭）
   6. 消息生成触觉反馈（默认：关闭）
   7. 生成消息后启用通知（默认：关闭；可选：关闭、实时、生成后）
   8. 跳过图片编辑（默认：开启）
   9. 显示助手消息气泡（默认：关闭）
   10. 气泡不透明度（默认：100%）
   11. 聊天列表模型图标（默认：开启）
   12. 显示思考内容（默认：开启）
   13. 自动折叠思考（默认：开启）
   14. 粘贴长文本为文件（默认：留空，即关闭）
   15. 音量键翻页（适合墨水屏）（默认：关闭；可选：关闭、25%、50%、75%、100%）
   16. AMOLED 暗色模式（默认：关闭）
2. “字体，公式和代码”分组
   1. 聊天字体（默认：系统默认字体）
   2. 自定义字体（默认：未导入）
   3. 全局使用自定义字体（默认：关闭；开启后整个 UI 跟随聊天字体）
   4. 聊天字体大小（默认：100%）
   5. 启用 LaTeX 渲染（默认：开启）
   6. 代码块自动换行（默认：关闭）
   7. 代码块自动折叠（默认：关闭）
   8. 显示行号（默认：关闭）
3. “TTS设置”分组
   1. TTS 仅朗读引用内容（默认：关闭）
   2. TTS 不朗读括号内的内容（默认：关闭）
   3. 生成后自动播放 TTS（默认：关闭）

## 已验证项目

- Release 版本可以编译。
- 侧栏各入口均可正常打开，聊天历史入口已移除。
- 搜索的精准匹配、模糊搜索和搜索模式持久化正常。
- 点击重试会立即显示等待中的新分支；连接失败且未产生内容时会删除空分支并切回原回复。
- 移动当前打开的聊天后会同时切换到目标助手和目标文件夹。
- 流式请求日志正常。
- 新安装的默认提供商列表正确。
- 热力图起始位置正确。
- 词数统计及模型价格设置、费用计算正常。
- 覆盖还原警告弹窗正常。
- 备份与导入正常。导出旧版zip正常。
- Web 服务器地址展示和复制正常。
- Web 服务器端口和密码输入框的字号、字重正常。
- mDNS 地址可以访问。
- 设置页整理及默认值正常。
- 思考 Markdown 复制正常。
- “显示 Token 和上下文统计”设置正常。
- 空白消息可以发送并以正常高度渲染。

## 尚待验证或长期观察

- 使用官方 APK 导入本版本导出的完整备份，确认新增的模型价格字段不会影响向后兼容。
- 日常使用时继续观察切换助手后首条消息是否仍有较低概率进入旧助手。
- 未测试导出的旧格式能否被原版RikkaHub读取。
- `生成图像`及`翻译`页 UI 正常，实际功能尚未测试。

## 兼容性说明

- 三份 README 均加入本修改记录入口；关于页底部保留英文修改与许可声明，Fork GitHub 指向 `atNyaChan/NyanChat`，并保留原项目 GitHub 及官网链接。
- 整个项目、Gradle Daemon toolchain、CI 与本地构建文档统一使用 JDK 21；各 Android 模块的 Java source/target compatibility 也统一为 21。
- 本地“导出为文件”同时提供“导出旧版格式（RikkaHub兼容）”；该 ZIP 沿用原项目布局，并在设置 JSON 中移除本 Fork 新增的模型价格字段。S3/WebDAV 仍只导出新格式。
